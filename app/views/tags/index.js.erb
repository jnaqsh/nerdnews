$('#tags-list').append('<%= j render(@tags) %>');
<% if @tags.next_page %>
  $(".pagination").replaceWith('<%= j paginate(@tags) %>');
<% else %>
  $('.pagination').remove();
<% end %>

<% sleep 1 %>

$("[rel=tooltip]").tooltip()
