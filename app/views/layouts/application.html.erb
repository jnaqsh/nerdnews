<!DOCTYPE html>
<html>
<head>
  <title>Open News</title>
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container">
          <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>

          <%= link_to t('.brand_name'), root_path, class: "brand" %>
          <!-- User Buttton -->
          <% unless current_user %>
          <div class="pull-left">
            <ul class="nav">
                <li><%= link_to t('.register'), new_user_path %></li>
                <li><%= link_to t('.login'), new_session_path %></li>
            </ul>
          </div>
          <% else %>
            <div class="btn-group pull-left">
              <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
                <i class="icon-user"></i> <%= current_user.full_name %>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t('.profile'), user_path(current_user) %></li>
                <li class="divider"></li>
                <% if can? :create, Page %>
                  <li><%= link_to t('.new_page'), new_page_path %></li>
                <% end %>
                <li><%= link_to t('.logout'), session_path, method: :delete,
                  data: {confirm: t("helpers.links.confirm")} %></li>
              </ul>
            </div>
          <% end %>
          <!-- Links -->
          <div class="nav-collapse">
            <ul class="nav">
              <li class="active"> <%= link_to t('.home'), root_path %> </li>
              <li><%= link_to t('.new_story'), new_story_path %></li>
              <li><%= link_to t('.about'), page_by_permalink_path("about-us") %></li>
              <li><%= link_to t('.contact us'), root_path %></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
<div class="container">
  <% flash.each do |name, msg| %>
    <div class="alert alert-<%= name == :notice ? 'success' : 'error' %> ">
      <a class="close" data-dismiss="alert">×</a>
      <%= msg %>
    </div>
  <% end %>
  <div class="row">
    <div class="span9">
      <%= yield %>
    </div>
    <div class="span3">
      <h2>درباره ما</h2>
      <p>هدف از ایجاد این سایت، اطلاع جدیدترین اخبار روز درباره آی‌تی است. این نرم‌افزار وب، یک نرم‌افزار آزاد است.</p>

      <% if @tags && !@tags.empty? %>
        <h2><%= t('.tags') %></h2>
        <% @tags.each do |tag| %>
          <%= link_to tag.name, stories_path(tag: tag.name) %>
        <% end %>
      <% end %>

    </div>
  </div>
</div>

</body>
</html>
